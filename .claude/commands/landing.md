Start the Amaxa landing page on the local dev server at http://localhost:4321.

Use the preview_start tool with the name "landing". The launch.json config already exists at .claude/launch.json with the correct settings (pnpm dev:landing, port 4321).

After starting, confirm the server is running and share the local URL with the user.
